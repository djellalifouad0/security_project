name: CI/CD Pipeline Simple

on:
  push:
    branches: [ main ]
  workflow_dispatch:

env:
  DOCKER_IMAGE_NAME: ${{ secrets.DOCKER_USERNAME }}/bibliotheque-app

jobs:
  # Job 1: Build, Scan et Test
  build-scan-test:
    name: Build, Scan Trivy et Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout du code
        uses: actions/checkout@v4

      - name: Configuration de Docker Buildx
        uses: docker/setup-buildx-action@v3

      # Build de l'image vulnérable
      - name: Build image vulnérable
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: false
          load: true
          tags: ${{ env.DOCKER_IMAGE_NAME }}:vulnerable
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # Scan Trivy de l'image vulnérable
      - name: Scan Trivy - Image Vulnérable (Table)
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'image'
          image-ref: ${{ env.DOCKER_IMAGE_NAME }}:vulnerable
          format: 'table'
          severity: 'CRITICAL,HIGH,MEDIUM'
          exit-code: '0'

      - name: Scan Trivy - Image Vulnérable (JSON)
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'image'
          image-ref: ${{ env.DOCKER_IMAGE_NAME }}:vulnerable
          format: 'json'
          output: 'trivy-report-vulnerable.json'
          severity: 'CRITICAL,HIGH,MEDIUM,LOW'
          exit-code: '0'

      - name: Scan Trivy - Image Vulnérable (HTML)
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'image'
          image-ref: ${{ env.DOCKER_IMAGE_NAME }}:vulnerable
          format: 'template'
          template: '@/contrib/html.tpl'
          output: 'trivy-report-vulnerable.html'
          severity: 'CRITICAL,HIGH,MEDIUM,LOW'
          exit-code: '0'

      # Build de l'image sécurisée
      - name: Build image sécurisée
        run: |
          cp app_secure.py app.py
          docker build -t ${{ env.DOCKER_IMAGE_NAME }}:secure .
          git checkout app.py

      # Scan Trivy de l'image sécurisée
      - name: Scan Trivy - Image Sécurisée (Table)
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'image'
          image-ref: ${{ env.DOCKER_IMAGE_NAME }}:secure
          format: 'table'
          severity: 'CRITICAL,HIGH,MEDIUM'
          exit-code: '0'

      - name: Scan Trivy - Image Sécurisée (JSON)
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'image'
          image-ref: ${{ env.DOCKER_IMAGE_NAME }}:secure
          format: 'json'
          output: 'trivy-report-secure.json'
          severity: 'CRITICAL,HIGH,MEDIUM,LOW'
          exit-code: '0'

      - name: Scan Trivy - Image Sécurisée (HTML)
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'image'
          image-ref: ${{ env.DOCKER_IMAGE_NAME }}:secure
          format: 'template'
          template: '@/contrib/html.tpl'
          output: 'trivy-report-secure.html'
          severity: 'CRITICAL,HIGH,MEDIUM,LOW'
          exit-code: '0'

      # Test de l'image vulnérable
      - name: Test image vulnérable
        run: |
          docker run -d -p 5000:5000 --name test-vulnerable ${{ env.DOCKER_IMAGE_NAME }}:vulnerable
          sleep 10
          curl -f http://localhost:5000/health || exit 1
          docker stop test-vulnerable
          docker rm test-vulnerable

      # Test de l'image sécurisée
      - name: Test image sécurisée
        run: |
          docker run -d -p 5000:5000 --name test-secure ${{ env.DOCKER_IMAGE_NAME }}:secure
          sleep 10
          curl -f http://localhost:5000/health || exit 1
          docker stop test-secure
          docker rm test-secure

      # Upload des rapports Trivy en tant qu'artefacts
      - name: Upload Rapports Trivy
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: trivy-reports
          path: |
            trivy-report-vulnerable.json
            trivy-report-vulnerable.html
            trivy-report-secure.json
            trivy-report-secure.html
          retention-days: 30

      - name: Résumé des scans
        run: |
          echo "## 🔒 Analyse de Sécurité Trivy" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "✅ **Image Vulnérable** : Scannée (vulnérabilités attendues)" >> $GITHUB_STEP_SUMMARY
          echo "✅ **Image Sécurisée** : Scannée (moins de vulnérabilités)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "📊 **Rapports disponibles** : Téléchargez les artefacts 'trivy-reports'" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Les deux images ont été testées avec succès !" >> $GITHUB_STEP_SUMMARY

  # Job 2: Push vers Docker Hub
  docker-push:
    name: Push vers Docker Hub
    runs-on: ubuntu-latest
    needs: build-scan-test
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Checkout du code
        uses: actions/checkout@v4

      - name: Configuration de Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Connexion à Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # Push image vulnérable
      - name: Build et Push - Image Vulnérable
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ${{ env.DOCKER_IMAGE_NAME }}:vulnerable
            ${{ env.DOCKER_IMAGE_NAME }}:latest
          cache-from: type=gha

      # Push image sécurisée
      - name: Build et Push - Image Sécurisée
        run: |
          cp app_secure.py app.py
          docker buildx build --push \
            -t ${{ env.DOCKER_IMAGE_NAME }}:secure \
            --platform linux/amd64,linux/arm64 \
            --cache-from type=gha .

      - name: Résumé de la publication
        run: |
          echo "## 🐳 Images Docker Publiées" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Images disponibles :" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**1. Version Vulnérable (démo) :**" >> $GITHUB_STEP_SUMMARY
          echo '```bash' >> $GITHUB_STEP_SUMMARY
          echo "docker pull ${{ env.DOCKER_IMAGE_NAME }}:vulnerable" >> $GITHUB_STEP_SUMMARY
          echo "docker pull ${{ env.DOCKER_IMAGE_NAME }}:latest" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**2. Version Sécurisée :**" >> $GITHUB_STEP_SUMMARY
          echo '```bash' >> $GITHUB_STEP_SUMMARY
          echo "docker pull ${{ env.DOCKER_IMAGE_NAME }}:secure" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
